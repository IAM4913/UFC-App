# Copy to .env (same folder as server.js). Never commit .env.
ANTHROPIC_API_KEY=sk-ant-...
# Optional overrides
# CHAT_MODEL=claude-opus-5
# CHAT_EFFORT=medium   # low | medium | high  (lower = faster answers on the clock)
# PORT=3000
